{
	"prdName": "dreamcli",
	"description": "Schema-first, fully typed TypeScript CLI framework. v0.1 core, v0.2 resolution chain, v0.3 interactive prompting, v0.4 middleware + structured output",
	"tasks": [
		{
			"id": "mw-1-context-type-param",
			"title": "Add context type parameter to ActionParams and CommandBuilder",
			"description": "Add third type parameter C to ActionParams, ActionHandler, and CommandBuilder for typed middleware context.",
			"passes": true,
			"priority": "architecture"
		},
		{
			"id": "middleware-types",
			"title": "Middleware type definitions and factory function",
			"description": "Define Middleware<In, Out> type, MiddlewareContext, next() signature. Create middleware() factory. Wire C type parameter through CommandBuilder.middleware() to widen context via intersection.",
			"priority": 1,
			"category": "architecture",
			"passes": true
		},
		{
			"id": "middleware-chain",
			"title": "Middleware chain execution in runCommand pipeline",
			"description": "Implement middleware chain runner: execute middlewares in order, each calling next() to pass context downstream. Wire into testkit runCommand() pipeline between resolve and action invocation. Handle errors thrown in middleware (CLIError propagation).",
			"priority": 1,
			"category": "architecture",
			"passes": true
		},
		{
			"id": "output-json",
			"title": "Extend Out interface with json() method and --json mode",
			"description": "Add json(value) to Out interface. Add --json global flag support. In --json mode: json() writes JSON to stdout, log/info/warn/error are suppressed or redirected.",
			"priority": 2,
			"category": "integration",
			"passes": true
		},
		{
			"id": "output-table",
			"title": "Table output with TTY-aware rendering",
			"description": "Add table(rows, columns) to Out. TTY mode: pretty-print aligned columns. Piped/--json mode: emit JSON array.",
			"priority": 2,
			"category": "integration",
			"passes": true
		},
		{
			"id": "output-tty",
			"title": "TTY detection and output mode inference",
			"description": "Wire isTTY from RuntimeAdapter through output channel creation. Ensure --json flag overrides TTY pretty-printing.",
			"priority": 2,
			"category": "integration",
			"passes": true
		},
		{
			"id": "cli-middleware-integration",
			"title": "Wire middleware through CLIBuilder and testkit execution",
			"description": "Integrate middleware chain into CLIBuilder.execute() and .run() paths. Ensure runCommand() in testkit supports middleware.",
			"priority": 3,
			"category": "integration",
			"passes": true
		},
		{
			"id": "cli-json-flag",
			"title": "Global --json flag on CLIBuilder",
			"description": "Add automatic --json flag to CLIBuilder. Pass jsonMode to output channel. Ensure errors also render as JSON in --json mode.",
			"priority": 3,
			"category": "feature",
			"passes": true
		},
		{
			"id": "middleware-context-e2e",
			"title": "End-to-end middleware + context type safety tests",
			"description": "Integration tests: middleware chain with multiple middlewares composing context, typed ctx in action handler, error middleware, middleware ordering.",
			"priority": 4,
			"category": "feature",
			"passes": true
		},
		{
			"id": "output-e2e",
			"title": "End-to-end output channel tests (json, table, TTY modes)",
			"description": "Integration tests: --json mode with json()/table()/log(), piped mode, TTY mode, mixed output.",
			"priority": 4,
			"category": "feature",
			"passes": true
		}
	]
}
