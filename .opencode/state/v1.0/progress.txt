# Progress Log

PRD: v1.0
Started: 2026-03-30

## Codebase Patterns
<!-- Consolidate reusable patterns here -->
- Public API is curated explicitly through `src/index.ts`, `src/testkit.ts`, and `src/runtime.ts` with named re-exports only
- Runtime compatibility work lives in `src/runtime/` and `src/runtime/detect.ts`; core modules stay runtime-agnostic behind `RuntimeAdapter`
- Cross-cutting CLI pipeline changes usually thread through `src/core/cli/index.ts`, `src/core/testkit/index.ts`, and `src/core/resolve/index.ts`
- CLI-wide lifecycle extensions live in `src/core/cli/plugin.ts` and are threaded through `RunOptions.plugins`; hooks wrap parse, resolve, and action stages outside command middleware in registration order

---
<!-- Task logs below - APPEND ONLY -->

## Task - [plugin-lifecycle-hooks]

- Implemented `CLIBuilder.plugin()` plus a public `plugin()` factory and typed lifecycle hook payloads for `beforeParse`, `afterResolve`, `beforeAction`, and `afterAction`
- Threaded registered plugins through CLI dispatch into `runCommand()` so hooks observe leaf-command execution without changing middleware semantics
- Added `src/core/cli/cli-plugin.test.ts` coverage for observation, per-stage ordering, and plugin/middleware coexistence; updated completion test helpers for the expanded `CLISchema`
- Files changed: `src/core/cli/plugin.ts`, `src/core/cli/index.ts`, `src/core/testkit/index.ts`, `src/core/cli/cli-plugin.test.ts`, `src/core/completion/completion.test.ts`, `src/index.ts`, `.opencode/state/v1.0/prd.json`, `.opencode/state/v1.0/progress.txt`
- **Learnings:** the safest seam is `RunOptions` because CLI dispatch already centralizes leaf-command metadata and testkit owns parse/resolve/action orchestration
