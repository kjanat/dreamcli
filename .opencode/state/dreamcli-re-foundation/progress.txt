# Progress Log

PRD: dreamcli-re-foundation
Started: 2026-04-02

## Codebase Patterns

- The real architectural pressure points are `src/core/cli/`, `src/core/testkit/`, `src/core/resolve/`, `src/core/output/`, and `src/core/completion/`.
- Current execution ownership is effectively centered in `runCommand()` even though the architecture does not present it that way.
- The safest refactor path is contract-first: truth matrix -> characterization tests -> shared executor -> planner split -> resolver redesign.
- When execution ownership is implicit, add a dedicated black-box contract suite at the current owner boundary before extraction; it lets later shared seams move without coupling tests to module shape.
- Docs work must split into low-risk generated foundations versus semantics-sensitive authored docs.
- `ROADMAP-DOCS-SICK.md` is the main north star for the generated docs platform: examples first, hover on examples only, changelog and docs health, then API inventory, then TypeDoc-based symbol pages.
- Completion breadth must be backed by real implementation and tests; fish and PowerShell are intentional backlog items, not aspirational claims.
- The GitHub project should mirror `prd.json` at mixed depth, with draft items rather than real repo issues.
- When a public type surface is broader than the real support surface, add an audited support-matrix page and make docs state the runtime behavior explicitly instead of staying silent.
- Before comitting, always run `dprint fmt`
- For extraction-first refactors, land a narrow internal contract module plus one reused helper before moving control flow; it gives tests and later module splits a concrete seam without forcing a full rewrite.
- For contract-definition tasks in hot modules, extract the contract to a sibling `contracts.ts` file and have the implementation consume those types; it makes the seam testable without moving behavior yet.
- For policy-heavy modules, extract pure policy helpers that return semantic mode/stream/cleanup facts, then make the renderer consume them; it separates contract from mechanics without forcing a big structural split.
- For planner characterization work, extract one pure `planInvocation()`-style helper that owns root interception and command-plan assembly, then test that seam directly instead of asserting through handlers.
- When subsystem helpers already aggregate their own `ValidationError`s, do cross-domain aggregation at the top boundary by flattening nested error JSON back into `ValidationError`s instead of weakening the lower-level helpers.
- For execution refactors, extract a minimal `executeCommand()` seam with explicit request/result types and a shared `buildRunResult()` helper first; keep CLI and testkit as thin bootstrap wrappers until routing work lands.
- For oversized hot modules, keep `index.ts` as the orchestration boundary and extract sibling concern modules first (`args`, `flags`, `coerce`, `config`, `errors`) before changing behavior; the contract tests stay stable while the file shape improves.
- When centralizing execution ownership, wrap the whole executor in one `try/finally` so fast exits like `--help` and missing-handler guards still clean up active output handles.
- When a new internal seam broadens a hot test helper interface, make the seam optional and keep the old path as a fallback for fake test doubles; production wiring can move first without forcing broad fixture churn.
- For output-layer policy refactors, freeze one `policy` snapshot per channel and route concrete writer or handle creation through sibling renderer helpers; that splits semantic mode choice from mechanics without changing the public `Out` surface.
- For CLI run-path refactors, extract adapter-driven config/package/stdin/prompt sourcing into a `runtime-preflight` helper that consumes planner outcomes; it removes duplicate dispatch branching while keeping `run()` as a thin runtime shell.
- Once a planner seam exists, pass raw argv into it and let the planner own global invocation normalization like root `--json`; keep `execute()` focused on output setup, rendering, and executor handoff.
- For generated-docs groundwork, keep source-backed build outputs under `docs/.generated/` and let authored wrapper pages plus VitePress config import one generated manifest; that preserves clean source boundaries while future docs generators can expand without reshaping IA again.
- For generated reference work, build one source-backed inventory model first and emit both JSON and markdown artifacts from it; authored wrapper pages can stay on stable routes while later normalization work reuses the same data seam.
- For TypeDoc-backed reference work, keep the public export inventory as coverage truth, generate raw `typedoc.json` beside a smaller DreamCLI-owned normalized model keyed by `entrypoint:symbol`, and force stable source links so generated artifacts do not churn on every commit.
- When flags and args partially overlap, share only the coercion-compatible property shape in a dedicated resolver helper and keep precedence, fallback, and required-value rules separate; broad shared property loops hide real semantic differences.
- For generated reference work, keep the heavy source-of-truth model under `docs/.generated/`, but emit lightweight searchable public routes from that model and keep VitePress metadata limited to route/index facts instead of full page payloads.
- For generated example docs, render full source-backed detail pages to `docs/examples/*.md`, but keep `docs/.generated/site-data.ts` limited to nav/index metadata; pushing full source blobs into VitePress config metadata creates avoidable churn.
- For resolver diagnostic redesign, keep nested `ValidationError` JSON stable and add a parallel per-issue summary at the aggregate wrapper; it clarifies mixed flag/arg failures without weakening existing leaf-error assertions.

## Project Tracking

- GitHub Project: https://github.com/users/kjanat/projects/4
- Project number: 4
- Draft items imported from `prd.json`: 33
- Custom tracking fields: Workflow, Phase, Task Priority, PRD Task ID

---

## Task - [support-truth-matrix]

- Implemented an audited support matrix at `docs/reference/support-matrix.md` covering execution semantics, runtimes, input sources, completions, output, testing, and docs surfaces.
- Wired the matrix into the VitePress reference sidebar and added direct links from `docs/guide/completions.md` and `docs/reference/api.md`.
- Tightened completion docs so they state the current behavior explicitly: `bash` and `zsh` are supported today; `fish` and `powershell` currently throw not-supported `CLIError`s.
- Files changed: `docs/reference/support-matrix.md`, `docs/.vitepress/config.ts`, `docs/guide/completions.md`, `docs/reference/api.md`, `.opencode/state/dreamcli-re-foundation/prd.json`, `.opencode/state/dreamcli-re-foundation/progress.txt`.
- Learnings: docs truth gaps are easiest to close when the matrix links straight to code and tests; completions were the highest-value honesty fix because the type surface is broader than the shipped shell surface.

## Task - [planner-contracts]

- Added an explicit internal planner contract module at `src/core/cli/planner.ts` with `DispatchOutcome`, `CommandExecutionPlan`, `OutputPolicy`, and shared schema-merging helpers.
- Routed CLI matched-command execution and stdin preflight through the shared planner helpers so propagated-flag merging now has one owned seam instead of duplicated inline logic.
- Added `docs/reference/planner-contract.md` and linked it from the reference nav so the re-foundation has a concrete planner contract page to anchor later tests and extraction work.
- Files changed: `src/core/cli/planner.ts`, `src/core/cli/index.ts`, `docs/reference/planner-contract.md`, `docs/.vitepress/config.ts`, `docs/reference/api.md`, `.opencode/state/dreamcli-re-foundation/prd.json`, `.opencode/state/dreamcli-re-foundation/progress.txt`.
- Learnings: the most reusable early planner seam is propagated-schema merging plus the matched-command handoff shape; those were the only duplicated planner concerns already present in production code.

## Task - [resolver-contracts]

- Added an explicit internal resolver contract module at `src/core/resolve/contracts.ts` covering invocation input, resolved output, precedence stages, source-aware diagnostic payloads, and frozen contract facts for later tests.
- Routed `src/core/resolve/index.ts` to consume the named contract types so resolver I/O and diagnostic source boundaries now live in one owned seam instead of staying embedded in the hot path file.
- Added `docs/reference/resolver-contract.md` and linked it from the reference nav and API overview so later resolver tests and decomposition work have one contract page to target.
- Files changed: `src/core/resolve/contracts.ts`, `src/core/resolve/index.ts`, `docs/reference/resolver-contract.md`, `docs/.vitepress/config.ts`, `docs/reference/api.md`, `.opencode/state/dreamcli-re-foundation/prd.json`, `.opencode/state/dreamcli-re-foundation/progress.txt`.
- Learnings: resolver contracts are easiest to stabilize by naming source/precedence facts in a sibling module first, then letting the main resolver file import them; that keeps future module splitting honest without forcing behavior changes early.

## Task - [output-contracts]

- Added an explicit internal output contract module at `src/core/output/contracts.ts` covering text routing, table routing, activity-mode selection, and cleanup semantics for spinners and progress handles.
- Routed `src/core/output/index.ts` and `src/core/cli/planner.ts` through the shared output policy types and helpers so output-policy decisions now live in one owned seam instead of staying embedded in rendering code.
- Added `src/core/output/contracts.test.ts` plus `docs/reference/output-contract.md`, and linked the new contract page from the reference nav, API overview, and support matrix.
- Files changed: `src/core/output/contracts.ts`, `src/core/output/contracts.test.ts`, `src/core/output/index.ts`, `src/core/cli/planner.ts`, `docs/reference/output-contract.md`, `docs/.vitepress/config.ts`, `docs/reference/api.md`, `docs/reference/support-matrix.md`, `.opencode/state/dreamcli-re-foundation/prd.json`, `.opencode/state/dreamcli-re-foundation/progress.txt`.
- Learnings: output-policy seams stabilize best when helpers return pure routing and cleanup facts first; once those facts exist, `OutputChannel` can consume them without changing the shipped behavior.

## Task - [planner-contract-tests]

- Extracted a pure `planInvocation()` helper in `src/core/cli/planner.ts` so root help/version/default-command planning, nested dispatch outcomes, and command-plan assembly can be validated without invoking real handlers.
- Added `src/core/cli/planner.test.ts` to lock down root interception, virtual-help rewriting, default-command fallback, nested unknown/needs-subcommand outcomes, propagated-flag masking, and `--json` extraction ordering.
- Routed `CLIBuilder.execute()` through the planner helper while preserving existing renderer behavior and feedback-loop output.
- Files changed: `src/core/cli/planner.ts`, `src/core/cli/index.ts`, `src/core/cli/planner.test.ts`, `.opencode/state/dreamcli-re-foundation/prd.json`, `.opencode/state/dreamcli-re-foundation/progress.txt`.
- Learnings: planner contract suites get much simpler once root interception and default-command fallback move behind one pure seam; the renderer can stay thin and behavior stays black-box testable.

## Task - [resolver-matrix-tests]

- Added `src/core/resolve/contracts.test.ts` to lock down the explicit resolver contract facts plus focused precedence matrices for flags (`cli -> env -> config -> prompt -> default`) and args (`cli -> stdin -> env -> default`).
- Fixed top-level `resolve()` aggregation so flag and arg validation failures are collected together, while preserving the existing lower-level flag-only and arg-only aggregation behavior.
- Files changed: `src/core/resolve/contracts.test.ts`, `src/core/resolve/index.ts`, `.opencode/state/dreamcli-re-foundation/prd.json`, `.opencode/state/dreamcli-re-foundation/progress.txt`.
- Learnings: the contract suite exposed that cross-domain aggregation was weaker than the documented resolver boundary; flattening nested aggregated errors at the top resolver seam fixed it without changing the inner resolver helpers.

## Task - [executor-contract-tests]

- Added `src/core/testkit/executor-contract.test.ts` to lock down `runCommand()` lifecycle hook ordering, derive/middleware/action execution boundaries, `afterAction` skip-on-failure semantics, cleanup guarantees, and `RunResult` assembly when callers inject output buffers.
- Files changed: `src/core/testkit/executor-contract.test.ts`, `.opencode/state/dreamcli-re-foundation/prd.json`, `.opencode/state/dreamcli-re-foundation/progress.txt`.
- Learnings: the narrowest executor seam today is still `runCommand()` itself; contract tests there cover plugin ordering and cleanup behavior without freezing the eventual shared executor module shape.

## Task - [scaffold-core-execution]

- Added `src/core/execution/index.ts` as the new internal executor seam with explicit `CommandExecutionRequest` and `CommandExecutionResult` contracts plus shared parse/resolve/plugin/handler orchestration.
- Slimmed `runCommand()` down to output/bootstrap work so it now delegates the canonical execution flow to `executeCommand()`, and reused `buildRunResult()` from the new module in both testkit and CLI root-level result assembly.
- Files changed: `src/core/execution/index.ts`, `src/core/testkit/index.ts`, `src/core/cli/index.ts`, `.opencode/state/dreamcli-re-foundation/prd.json`, `.opencode/state/dreamcli-re-foundation/progress.txt`.
- Learnings: the safest first extraction is the semantic executor core, not full caller rewiring; once request/result types exist, later CLI and testkit routing work can shrink to adapter changes instead of re-extracting behavior.

## Task - [split-resolver-modules]

- Split the resolver into focused sibling modules so `src/core/resolve/index.ts` now only orchestrates top-level flag/arg resolution and aggregated error handling.
- Extracted `src/core/resolve/flags.ts`, `src/core/resolve/args.ts`, `src/core/resolve/coerce.ts`, `src/core/resolve/config.ts`, and `src/core/resolve/errors.ts` to separate flag policy, arg policy, shared coercion, config lookup, and validation aggregation concerns.
- Files changed: `src/core/resolve/index.ts`, `src/core/resolve/flags.ts`, `src/core/resolve/args.ts`, `src/core/resolve/coerce.ts`, `src/core/resolve/config.ts`, `src/core/resolve/errors.ts`, `.opencode/state/dreamcli-re-foundation/prd.json`, `.opencode/state/dreamcli-re-foundation/progress.txt`.
- Learnings: keeping the public `resolve()` seam in place while extracting sibling helper modules is the lowest-risk way to break up a policy-heavy hot file; the existing resolver contract suite stays useful without any test rewrites.

## Task - [route-testkit-executor]

- Tightened `src/core/execution/index.ts` so output cleanup now runs for all executor exit paths, including `--help` and missing-action early returns.
- Reframed `runCommand()` and related docs as a thin in-process wrapper over the shared executor, and updated the support matrix to reflect that the canonical execution flow now lives in `src/core/execution/index.ts`.
- Added contract coverage proving injected output handles are cleaned up on help exits and missing-action failures while existing testkit behavior stays green.
- Files changed: `src/core/execution/index.ts`, `src/core/testkit/index.ts`, `src/core/testkit/executor-contract.test.ts`, `src/core/schema/run.ts`, `src/core/testkit/AGENTS.md`, `docs/reference/support-matrix.md`, `.opencode/state/dreamcli-re-foundation/prd.json`, `.opencode/state/dreamcli-re-foundation/progress.txt`.
- Learnings: once testkit delegates execution, the remaining honesty work is mostly boundary truthing plus contract tests for wrapper-sensitive cases like capture cleanup on early executor exits.

## Task - [route-cli-executor]

- Routed matched CLI command execution straight to `executeCommand()` instead of bouncing through `runCommand()`, while keeping `_execute()` as a fallback for fake internal `ErasedCommand` test doubles.
- Added CLI-level coverage proving injected output handles still clean up on matched-command failures, and updated the support matrix to say both CLI and testkit now delegate directly to the shared executor.
- Files changed: `src/core/cli/index.ts`, `src/core/cli/cli.test.ts`, `src/core/schema/command.ts`, `docs/reference/support-matrix.md`, `.opencode/state/dreamcli-re-foundation/prd.json`, `.opencode/state/dreamcli-re-foundation/progress.txt`.
- Learnings: moving the real CLI path first is low-risk if the erased-command seam can carry the raw builder optionally; internal fake commands can stay lightweight while production dispatch stops depending on testkit.

## Task - [decouple-output-policy]

- Added `src/core/output/renderers.ts` as a narrow internal renderer/factory seam so concrete stdout/stderr writer selection and spinner/progress handle construction no longer live inline in `OutputChannel`.
- Made `OutputChannel` hold an explicit `policy` snapshot, routed log/info/table/activity decisions through that policy, and updated the output contract doc to reflect the new split while keeping the public API unchanged.
- Files changed: `src/core/output/renderers.ts`, `src/core/output/contracts.ts`, `src/core/output/index.ts`, `src/core/output/contracts.test.ts`, `src/core/output/output.test.ts`, `docs/reference/output-contract.md`, `.opencode/state/dreamcli-re-foundation/prd.json`, `.opencode/state/dreamcli-re-foundation/progress.txt`.
- Learnings: the smallest safe output split is policy snapshot plus renderer helpers; capture can stay subclass-based while normal output stops branching on concrete handle classes inline.

## Task - [extract-runtime-preflight]

- Added `src/core/cli/runtime-preflight.ts` as the explicit runtime sourcing seam for `CLIBuilder.run()`, owning config-path stripping, package/config discovery, prompt auto-wiring, and invocation-scoped stdin reads.
- Slimmed `CLIBuilder.run()` down to adapter setup, preflight error rendering, output creation, and the final handoff into `execute()`.
- Added `src/core/cli/runtime-preflight.test.ts` to lock down package/config discovery, completions skip behavior, stdin gating, prompt gating, and config-error outcomes.
- Files changed: `src/core/cli/runtime-preflight.ts`, `src/core/cli/runtime-preflight.test.ts`, `src/core/cli/index.ts`, `.opencode/state/dreamcli-re-foundation/prd.json`, `.opencode/state/dreamcli-re-foundation/progress.txt`.
- Learnings: using `planInvocation()` inside preflight is the cleanest way to decide stdin/completions behavior without re-encoding dispatch rules in the runtime layer.

## Task - [extract-invocation-planner]

- Tightened the planner boundary so `CLIBuilder.execute()` now passes raw argv into `planInvocation()`, leaving global `--json` normalization owned by `src/core/cli/planner.ts` instead of duplicated at the CLI shell.
- Updated planner-facing docs and test wording to reflect that invocation planning is now the explicit seam, while `CLIBuilder.execute()` is just the render-and-execute shell over planner outcomes.
- Files changed: `src/core/cli/index.ts`, `src/core/cli/planner.ts`, `src/core/cli/cli-json.test.ts`, `docs/reference/planner-contract.md`, `.opencode/state/dreamcli-re-foundation/prd.json`, `.opencode/state/dreamcli-re-foundation/progress.txt`.
- Learnings: once planner outcomes are explicit, the last cleanup step is usually ownership cleanup rather than structural rewrite; duplicated argv shaping is enough to keep the boundary blurry.

## Task - [build-docs-prepare]

- Added `scripts/docs/build-docs-data.ts` plus shared path helpers to rebuild the docs foundation under `docs/.generated/` from repo source inputs.
- Wired `docs:prepare` into `docs:dev` and `docs:build`, added authored wrapper pages for `/examples/` and `/reference/generated-surfaces`, and made VitePress consume the generated manifest so docs wiring now depends on prepared artifacts.
- Seeded stable generated artifacts for examples inventory, changelog mirroring, docs-health counts, and public export inventory under `docs/.generated/`.
- Files changed: `scripts/docs/build-docs-data.ts`, `scripts/docs/shared/paths.ts`, `package.json`, `docs/.vitepress/config.ts`, `docs/examples/index.md`, `docs/reference/generated-surfaces.md`, `docs/.generated/site-data.ts`, `docs/.generated/examples/index.md`, `docs/.generated/reference/changelog.md`, `docs/.generated/reference/docs-health.md`, `docs/.generated/api/public-exports.json`, `.opencode/state/dreamcli-re-foundation/progress.txt`.
- Learnings: one generated `site-data.ts` manifest is the cleanest early seam for VitePress; config and wrapper pages can depend on prepared data now, while later example/reference generators can keep emitting into `.generated/` without another nav rewrite.

## Task - [generate-api-index]

- Replaced the placeholder public-export manifest with a real source-backed API inventory that resolves every public symbol from the package entrypoints, groups them by subpath and kind, and emits both structured JSON and a generated markdown index.
- Turned `docs/reference/api.md` into a generated-data wrapper page so the stable `/reference/api` route now shows the full grouped inventory instead of a hand-maintained highlights list, and updated generated-surface/docs-health snapshots to expose entrypoint and symbol counts.
- Added `scripts/docs/shared/api-index.test.ts` to lock down key symbol classification and rendered index output against the live public entrypoints.
- Files changed: `scripts/docs/shared/api-index.ts`, `scripts/docs/shared/api-index.test.ts`, `scripts/docs/shared/paths.ts`, `scripts/docs/build-docs-data.ts`, `docs/reference/api.md`, `docs/reference/generated-surfaces.md`, `docs/.generated/site-data.ts`, `docs/.generated/api/index.md`, `docs/.generated/api/public-exports.json`, `docs/.generated/reference/docs-health.md`, `.opencode/state/dreamcli-re-foundation/prd.json`, `.opencode/state/dreamcli-re-foundation/progress.txt`.
- Learnings: using the TypeScript checker against the public entrypoint barrels is the smallest reliable way to inventory real exports across re-export chains; syntax-only parsing misses local export lists like `export { CLIBuilder, cli, plugin }`.

## Task - [normalize-typedoc-data]

- Added `scripts/docs/shared/typedoc.ts` to generate raw TypeDoc JSON from the public TS entrypoints and normalize matched exports into a DreamCLI-owned docs model keyed by `entrypoint:symbol`.
- Wired `scripts/docs/build-docs-data.ts` and `scripts/docs/shared/paths.ts` to emit `docs/.generated/api/typedoc.json` plus `docs/.generated/api/typedoc-normalized.json`, updated generated-surface/docs-health metadata, and taught `vitest.config.ts` to run docs-script tests.
- Added `scripts/docs/shared/typedoc.test.ts`, updated `docs/reference/api.md`, and fixed the `cli()` factory TSDoc so the new docs path runs without steady TypeDoc warning noise.
- Files changed: `package.json`, `bun.lock`, `scripts/docs/shared/typedoc.ts`, `scripts/docs/shared/typedoc.test.ts`, `scripts/docs/shared/paths.ts`, `scripts/docs/build-docs-data.ts`, `vitest.config.ts`, `docs/reference/api.md`, `docs/.generated/api/typedoc.json`, `docs/.generated/api/typedoc-normalized.json`, `docs/.generated/reference/docs-health.md`, `docs/.generated/site-data.ts`, `src/core/cli/index.ts`, `.opencode/state/dreamcli-re-foundation/prd.json`, `.opencode/state/dreamcli-re-foundation/progress.txt`.
- Learnings: raw TypeDoc output is useful as an audit artifact, but later rendering work should consume the normalized model keyed by public export coverage; otherwise every symbol-page step stays coupled to TypeDoc's raw reflection shape and source-link churn.

## Task - [shared-property-model]

- Added `src/core/resolve/property.ts` as the explicit shared property-model seam for resolver coercion, with a contract that keeps precedence, fallback, and required-value behavior out of the abstraction.
- Routed arg coercion through the new shared property adapters instead of fabricating a temporary `FlagSchema`, and split common scalar coercion in `src/core/resolve/coerce.ts` from flag-only boolean/array handling.
- Documented the decision in `docs/reference/resolver-contract.md` and added `src/core/resolve/property.test.ts` to lock the scope down.
- Files changed: `src/core/resolve/property.ts`, `src/core/resolve/property.test.ts`, `src/core/resolve/coerce.ts`, `docs/reference/resolver-contract.md`, `.opencode/state/dreamcli-re-foundation/prd.json`, `.opencode/state/dreamcli-re-foundation/progress.txt`.
- Learnings: the only shared flag/arg model justified today is coercion metadata for overlapping kinds; pushing precedence or fallback policy into the same abstraction would make the resolver harder to reason about, not easier.

## Task - [render-symbol-pages]

- Added `scripts/docs/shared/symbol-pages.ts` plus `scripts/docs/shared/symbol-pages.test.ts` so `bun run docs:prepare` now renders stable per-symbol reference pages under `docs/reference/symbols/` from the normalized TypeDoc model.
- Linked the generated API inventory to the new symbol routes, surfaced symbol-page counts in the reference wrappers, and updated docs truth so the support matrix now treats generated API index plus symbol pages as shipped instead of deferred.
- Files changed: `scripts/docs/shared/symbol-pages.ts`, `scripts/docs/shared/symbol-pages.test.ts`, `scripts/docs/shared/api-index.ts`, `scripts/docs/shared/api-index.test.ts`, `scripts/docs/shared/paths.ts`, `scripts/docs/build-docs-data.ts`, `docs/reference/api.md`, `docs/reference/generated-surfaces.md`, `docs/reference/support-matrix.md`, `docs/.generated/api/index.md`, `docs/.generated/reference/docs-health.md`, `docs/.generated/site-data.ts`, `docs/reference/symbols/**/*.md`, `.opencode/state/dreamcli-re-foundation/prd.json`, `.opencode/state/dreamcli-re-foundation/progress.txt`.
- Learnings: VitePress-facing metadata should stay small; exporting full generated page bodies through `site-data.ts` creates lint noise and couples wrapper pages to content they do not need.

## Task - [redesign-error-agg]

- Redesigned resolver aggregate errors so mixed failures now render per-issue summaries with normalized `flag --name` and `argument <name>` labels plus source labels like `env PORT` and `stdin` when the failing source is known.
- Added a dedicated aggregate-diagnostics suite at `src/core/resolve/resolve-aggregation.test.ts`, updated resolver contract facts, and documented the aggregate-summary requirement in `docs/reference/resolver-contract.md`.
- Files changed: `src/core/resolve/errors.ts`, `src/core/resolve/resolve-aggregation.test.ts`, `src/core/resolve/contracts.ts`, `src/core/resolve/contracts.test.ts`, `docs/reference/resolver-contract.md`, `.opencode/state/dreamcli-re-foundation/prd.json`, `.opencode/state/dreamcli-re-foundation/progress.txt`.
- Learnings: the least disruptive aggregation redesign is additive: keep nested error JSON untouched for existing consumers, and hang clearer per-issue summaries off the aggregate wrapper instead of reshaping leaf diagnostics.

## Task - [generate-example-pages]

- Added `scripts/docs/shared/examples.ts` plus `scripts/docs/shared/examples.test.ts` so `bun run docs:prepare` now collects source-backed example metadata, renders a linked inventory, and emits one generated page per top-level example under `docs/examples/`.
- Wired the examples sidebar and examples overview page to the generated routes, and made each example page include summary, usage snippets, GitHub source links, related API links, and the full source listing.
- Files changed: `scripts/docs/shared/examples.ts`, `scripts/docs/shared/examples.test.ts`, `scripts/docs/shared/paths.ts`, `scripts/docs/build-docs-data.ts`, `docs/.vitepress/config.ts`, `docs/examples/index.md`, `docs/.generated/examples/index.md`, `docs/.generated/reference/docs-health.md`, `docs/.generated/site-data.ts`, `docs/examples/*.md`, `.opencode/state/dreamcli-re-foundation/prd.json`, `.opencode/state/dreamcli-re-foundation/progress.txt`.
- Learnings: example pages fit the existing docs pipeline best when generation owns the detail routes outright and derives related links from imported public symbols; the authored overview page can stay thin while generated pages carry the searchable content.
