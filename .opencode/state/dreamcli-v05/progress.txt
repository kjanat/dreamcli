# Progress Log
PRD: dreamcli-v05
Started: 2026-02-09

## Codebase Patterns
<!-- Consolidate reusable patterns here -->
- **Module structure**: each core module has `index.ts` barrel, tests co-located as `*.test.ts`
- **Stub convention**: empty modules export `export {};` with JSDoc module comment
- **Type exports**: use `export type` (verbatimModuleSyntax), `.js` extensions in imports
- **CLISchema.commands[].schema**: `CommandSchema` has `.name`, `.flags` (Record<string, FlagSchema>), `.args` (CommandArgEntry[])
- **FlagSchema**: `.kind`, `.aliases`, `.description`, `.enumValues`
- **Error pattern**: throw `CLIError` for user-facing errors (not raw Error)
- **Test conventions**: em-dash in describes, `// ===` major separators, `// ---` minor, zero lifecycle hooks
- **GlobalThis injection**: use optional `globals` param (typed interface) instead of `vi.stubGlobal` for testing globalThis-dependent code — avoids cleanup/mutation issues
- **Runtime detection**: `GlobalForDetect` interface, check Bun before Node (Bun mimics Node's `process`)
- **Bash string literals**: biome flags `${...}` in single-quoted strings as `noTemplateCurlyInString` — suppress with `biome-ignore` when generating bash syntax
- **ErasedCommand test stubs**: `_execute` must return `RunResult` shape including `error: undefined`; use typed `[] as string[]` to avoid `never[]`
- **FlagKind union**: `'string' | 'number' | 'boolean' | 'enum' | 'array'` — no `'count'` variant
- **Auto-adapter factory**: `createAdapter(globals?)` in `auto.ts` — separate from `detect.ts` to keep detection deps-free; exhaustive switch on `Runtime` union for compile-time safety
- **Built-in subcommands**: use `command()`/`flag()` builders internally inside `CLIBuilder` methods, capture `this.schema` for schema access; schema snapshot is pre-registration (excludes the built-in command itself from generated output)

---
<!-- Task logs below - APPEND ONLY -->

## Task — completion-types
- Defined `Shell` type (`'bash' | 'zsh' | 'fish' | 'powershell'`), `SHELLS` constant, `CompletionOptions` interface
- Implemented `generateCompletion()` dispatcher, `generateBashCompletion()`, `generateZshCompletion()` as stubs throwing `CLIError` with `UNSUPPORTED_OPERATION` code
- Unsupported shells (fish/powershell) throw "not yet supported"; implemented shells throw "not yet implemented"
- 12 tests covering SHELLS constant, CompletionOptions type contract, all stub throws, dispatcher delegation, options passthrough
- Files: `src/core/completion/index.ts`, `src/core/completion/completion.test.ts`, `src/index.ts`
- **Learnings:** `ErrorCode` is open (`string & {}`) so any code string works; `void expr` pattern silences unused-param lint for stub args; `CLISchema` imports from `../cli/index.js` (no circular dep concern since completion doesn't feed back into cli yet)

## Task — runtime-detect
- Implemented `detectRuntime()` in `src/runtime/detect.ts` using `globalThis` feature detection
- Defined `Runtime` type (`'node' | 'bun' | 'deno' | 'unknown'`) and `RUNTIMES` constant
- Defined `GlobalForDetect` interface for injectable test globals (avoids mutating real `globalThis`)
- Detection order: Bun > Deno > Node > unknown (Bun checked first because it provides Node-compatible `process`)
- 18 tests covering: all runtimes, priority order, partial/missing globals, default globalThis fallback
- Exported from runtime barrel + main barrel (types: `Runtime`, `GlobalForDetect`; values: `detectRuntime`, `RUNTIMES`)
- Files: `src/runtime/detect.ts`, `src/runtime/detect.test.ts`, `src/runtime/index.ts`, `src/index.ts`
- **Learnings:** `as const satisfies` is compile-time only — `Object.isFrozen` returns false at runtime; the `globals` injection param pattern avoids needing `vi.stubGlobal`/cleanup for globalThis mutation

## Task — completion-bash
- Implemented `generateBashCompletion()` — pure string generation, no shell exec
- Script structure: shebang → `_<name>_completions()` → `_init_completion` → enum case → subcmd detection → per-command flags → root completions → `complete -F` registration
- Internal helpers: `sanitizeBashIdentifier()`, `collectFlagWords()`, `collectEnumCases()`
- Hidden commands excluded from completions; aliases included in case patterns
- Enum flags get `case "$prev"` value-completion branches
- `functionPrefix` option overrides function name; CLI name used for `complete` line
- 29 tests covering: script structure, shebang/header, function name, `complete -F` line, subcommand listing, hidden exclusion, alias detection, flag long/short forms, enum value completion, name sanitization, dispatcher delegation, options passthrough
- Rewrote test helpers: `flagSchema()`, `commandSchema()`, `erased()` factories for type-safe test `CLISchema` construction
- Files: `src/core/completion/index.ts`, `src/core/completion/completion.test.ts`
- **Learnings:** biome `noTemplateCurlyInString` triggers on bash `${words[i]}` in single-quoted JS strings — suppress per-line; `ErasedCommand._execute` return must include `error` field to satisfy `RunResult`

## Task — completion-zsh
- Implemented `generateZshCompletion()` — pure string generation, no shell exec
- Script structure: `#compdef` directive → `_<name>()` function → `_arguments -C` with `->state` dispatch → `_describe` for subcommand list → per-command `_arguments` with flag specs
- Internal helpers: `sanitizeZshIdentifier()`, `escapeZshDescription()`, `buildZshFlagSpecs()`
- `_arguments` flag specs: boolean flags get no value part; string/number get `:value:`; enum get `:value:(v1 v2 v3)`
- Short alias flags use zsh mutual exclusion group syntax: `(-s --long)'{-s,--long}'[desc]`
- Hidden commands excluded; aliases in case patterns; colons escaped in descriptions
- `functionPrefix` overrides function name; `#compdef` always uses original CLI name
- No-commands path emits simpler `_arguments` with only `--help`/`--version`
- Updated dispatcher test: zsh no longer throws, returns actual script
- 25 new zsh tests + 1 updated dispatcher test (54 completion tests total, 1082 suite-wide)
- Files: `src/core/completion/index.ts`, `src/core/completion/completion.test.ts`
- **Learnings:** `FlagKind` is `'string' | 'number' | 'boolean' | 'enum' | 'array'` — no `'count'` kind; zsh `_arguments` uses `->state` for subcommand dispatch with `case "$state"`; local vars (`subcmds`, `line`, `state`) should only be declared when commands exist to keep no-commands path clean

## Task — runtime-bun
- Implemented `createBunAdapter()` in `src/runtime/bun.ts` — delegates to `createNodeAdapter()`
- Bun provides Node-compatible `process` global, so full delegation is correct; separate factory establishes extension point for future Bun-specific optimizations (e.g. `Bun.write`, `Bun.stdin`)
- Accepts optional `NodeProcess` param for testability (same pattern as Node adapter)
- 13 tests covering: RuntimeAdapter contract satisfaction, process field delegation (argv, env, cwd, stdout, stderr, exit), TTY detection (stdout + stdin), stdin ReadFn type
- Exported from runtime barrel + main barrel (value: `createBunAdapter`)
- Files: `src/runtime/bun.ts`, `src/runtime/bun.test.ts`, `src/runtime/index.ts`, `src/index.ts`
- **Learnings:** delegation adapters should still have their own test file — verifies the contract independently even when implementation is trivial; `mockProcess()` helper with `Partial<NodeProcess>` overrides is cleaner than constructing full objects per test

## Task — completion-dispatch
- No code changes needed — `generateCompletion()` was already implemented incrementally during completion-types/completion-bash/completion-zsh tasks
- Dispatcher at `src/core/completion/index.ts:61-73`: switch on Shell, delegates bash/zsh, throws CLIError(UNSUPPORTED_OPERATION) for fish/powershell
- Already exported from main barrel (`src/index.ts:15`) as both value and type
- 6 existing tests in `completion.test.ts:695-753` cover: bash delegation, zsh delegation, fish/powershell CLIError throws, options passthrough
- All 1095 tests pass, type check/lint/format clean
- Files: none changed (only PRD + progress updated)
- **Learnings:** incremental implementation during prerequisite tasks can complete downstream tasks early — verify before writing duplicate code

## Task — runtime-auto
- Implemented `createAdapter()` in new `src/runtime/auto.ts` — uses `detectRuntime()` to dispatch: bun→`createBunAdapter()`, deno/node/unknown→`createNodeAdapter()`
- Deno falls through to Node adapter (no Deno adapter yet) — comment marks future extension point
- Accepts optional `GlobalForDetect` param for testability (same injection pattern as `detectRuntime`)
- Replaced `createNodeAdapter()` in `CLIBuilder.run()` with `createAdapter()` — auto-detection is now the default
- 11 tests: adapter shape validation per runtime, unknown fallback, default globalThis, functional checks (stdout/stderr callable, env populated, cwd non-empty)
- Exported from runtime barrel + main barrel (value: `createAdapter`)
- Files: `src/runtime/auto.ts` (new), `src/runtime/auto.test.ts` (new), `src/runtime/index.ts`, `src/index.ts`, `src/core/cli/index.ts`
- **Learnings:** auto-adapter factory belongs in separate `auto.ts` not `detect.ts` — keeps detection pure (no adapter deps) and factory composable; exhaustive switch on `Runtime` union ensures compile-time safety when new runtimes are added

## Task — completion-command
- Added `.completions()` method to `CLIBuilder` — registers built-in `completions` subcommand
- Subcommand has `--shell` flag (enum: `bash`/`zsh`, required), generates completion script via `generateCompletion()` and outputs via `out.log()`
- Schema snapshot design: captures `this.schema` at `.completions()` call time — completions command itself excluded from generated script; commands registered after `.completions()` excluded (documented)
- No `as Shell` cast needed — `'bash' | 'zsh'` is subtype of `Shell`, assignable directly
- 17 tests in `cli-completions.test.ts`: builder registration (3), bash output (4), zsh output (3), error handling (3), schema snapshot behavior (3), root help integration (1)
- All 1123 tests pass, typecheck/lint/format/build clean
- Files: `src/core/cli/index.ts`, `src/core/cli/cli-completions.test.ts` (new)
- **Learnings:** `flag.enum(['bash','zsh'] as const)` infers `'bash' | 'zsh'` which is a subtype of `Shell` — no type assertion needed for `generateCompletion()`; built-in subcommands can use `command()`/`flag()` directly inside CLIBuilder since they're builder-internal, not user-facing type complexity
