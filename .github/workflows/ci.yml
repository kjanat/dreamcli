name: CI
run-name: "CI - ${{ github.workflow }} - ${{ github.ref_name }} - ${{ github.event_name }} by ${{ github.actor }}"

on: { push: { branches: [master] }, pull_request: { branches: [master] } }
concurrency: { group: "ci-${{ github.ref }}", cancel-in-progress: true }
env: { RUNNER_PM: bun }

jobs:
  lint:
    name: static checks
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v7
      - uses: kjanat/runner@master
      - uses: oven-sh/setup-bun@v2
      - run: runner install --frozen version:check
      - run: run -pk typecheck typecheck:tsc meta-descriptions:check lint format:check typecheck:gh

  test:
    name: test (node ${{ matrix.node-version }}, bun latest)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy: { matrix: { node-version: [22.22.2, latest] }, fail-fast: false }
    steps:
      - uses: actions/checkout@v7
      - uses: kjanat/runner@master
      - uses: oven-sh/setup-bun@v2
        with: { bun-version: latest }
      - uses: actions/setup-node@v6
        with: { node-version: "${{ matrix.node-version }}" }
      - run: runner install --frozen
      - name: Test in parallel (Bun) (Node ${{ matrix.node-version }})
        run: run -pk test:bun test:gh test:node

  coverage:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [lint]
    steps:
      - uses: actions/checkout@v7
      - uses: kjanat/runner@master
      - uses: oven-sh/setup-bun@v2
      - uses: actions/setup-node@v6
        with: { node-version: latest }
      - run: runner install --frozen coverage

  test-deno:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [lint]
    steps:
      - uses: actions/checkout@v7
      - uses: kjanat/runner@master
      - uses: oven-sh/setup-bun@v2
      - uses: denoland/setup-deno@v2
      - run: runner install --frozen build smoke vitest:deno

  docs:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [lint]
    steps:
      - uses: actions/checkout@v7
      - uses: kjanat/runner@master
      - uses: oven-sh/setup-bun@v2
      - run: runner install --frozen docs:build
      - run: git diff --exit-code && test -z "$(git status --porcelain --untracked-files=all)"

  build:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [lint, test, coverage, test-deno, docs]
    steps:
      - uses: actions/checkout@v7
      - uses: oven-sh/setup-bun@v2
      - uses: kjanat/runner@master
      - run: runner install --frozen build
